# Research Report: Project Deep Dive & Optimization Analysis

이 프로젝트의 내부 구조와 동작 방식을 '레이'의 관점에서 정밀 분석하고, 웹 및 모바일에 최적화된 고도화 방안을 제시함.

## 1. 프로젝트 아키텍처 및 동작 원리

### A. 핵심 데이터 흐름 (Core Data Flow)
- **상태 관리**: `App.tsx`가 아닌 `useCalendarLogic.ts` 커스텀 훅이 모든 도메인 로직을 관장함. 날짜 변경(`selectedDate`)이 발생하면 이에 종속된 모든 데이터(주역 卦/爻, 영혼의 달력)가 `useMemo`와 순수 함수들을 통해 즉각적으로 리액티브하게 계산됨.
- **로직 분리**: 수학적 매핑(`dateUtils.ts`), 텍스트 파싱(`soulLogic.ts`), 일반 유틸(`textUtils.ts`)로 로직이 원자화되어 있어 유지보수성이 극대화됨.
- **데이터 구조**: `guaData.ts`, `yaoData.ts`, `soulData.ts`에 저장된 원본 텍스트를 인덱싱 맵(Map)으로 변환하여 O(1)의 성능으로 접근함.

### B. UI/UX 디자인 시스템 (Meta-Design Aesthetics)
- **글래스모피즘**: `glass-card` 클래스와 `backdrop-blur-xl`을 통해 현대적이고 투명한 레이어를 구현함.
- **골드 글로우**: 다중 레이어 섀도우를 통해 프리미엄한 입체감을 부여함.
- **애니메이션**: `fadeInUp`과 `stagger`를 결합하여 요소들이 리드미컬하게 등장하는 '시퀀스 엔트리'를 구현함.

## 2. 세부 구성 요소 분석 (Component Breakdown)

| 컴포넌트 | 기능 | 최적화 상태 |
| :--- | :--- | :--- |
| `Header` | 반응형 레이아웃 스왑 (`order` 활용), 다크모드 제어 | **양호** (웹/모바일 배치 분리됨) |
| `DatePicker` | 커스텀 날짜 선택기, 일수 계산 로직 연동 | **보통** (키보드 내비게이션 미흡) |
| `MainContent` | 주역 및 영혼의 달력 렌더링, 섹션별 애니메이션 | **비대** (파일 분리 필요) |
| `JournalModal` | 로컬 스토리지 기반 성찰 일기 작성 | **양호** (레이지 이니셜라이저 적용됨) |

## 3. 웹 & 모바일 최적화 전략 (Optimization Opportunities)

### A. 모바일 (Mobile First UX)
- **터치 타겟**: 버튼 크기는 적절하나, 활성 상태(`:active`)에서의 시각적 피드백을 강화하여 더 쫀득한 터치감을 제공해야 함.
- **레이아웃**: `MainContent`의 패딩을 모바일 화면비에 맞춰 더 정교하게 조정하여 가두리 양식 형태를 탈피해야 함.
- **모달 애니메이션**: 모바일 환경에서 슬라이드 업(`Slide Up`) 방식의 진입이 더 자연스러울 수 있음.

### B. 웹 (Desktop Premium Interactivity)
- **입체감 강화**: 마우스 움직임에 반응하는 `Tilt` 효과나 마우스 커서 이펙트를 고려하여 인터랙티브함을 배가시킴.
- **단축키**: 날짜 탐색(Today, 방향키 등)을 위한 키보드 이벤트를 전역에서 캡처하여 생산성 향상.
- **이미지 로딩**: 시길(Sigil) 이미지에 `aspect-ratio`를 강제하여 레이아웃 시프트를 미연에 방지.

### C. 퍼포먼스 & 코드 품질
- **컴포넌트 분화**: `MainContent.tsx`를 `IChingSection`, `SoulCalendarSection` 등으로 쪼개어 '제로 모놀리스' 원칙을 완벽히 이행.
- **SEO**: 메타 태그(`og:title`, `og:image` 등)를 강화하여 공유 시 프리미엄한 브랜드 가치 전달.
- **에셋**: `public/images/` 내의 PNG 파일을 WebP로 압축하여 로딩 성능 개선 검토.

---
분석 결과, 현재 프로젝트는 견고한 로직 위에 구축되어 있으나 UI 컴포넌트의 비대화와 모바일 인터랙션 디테일에서 고도화의 여지가 있음. 다음 단계에서 이를 반영한 `plan.md`를 작성함.
