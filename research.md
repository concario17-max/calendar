# Research Report: UI/UX Regression and Refinement Analysis

## 1. 개요 (Overview)
지난 UI/UX 고도화 작업 이후 발생한 의도치 않은 레이아웃 변경(웹 헤더)과 모바일 사용성 개선 사항, 그리고 데이터 텍스트 오류를 분석합니다.

## 2. 주요 문제점 및 분석 (Key Issues)

### A. 웹 헤더 레이아웃 회귀 (Web Header Regression)
- **현상**: 모바일 최적화 과정에서 웹 버전의 헤더 레이아웃까지 변경됨 (좌측 타이틀, 우측 컨트롤).
- **사용자 요청**: 웹 버전은 이전 상태(아마도 좌측 날짜 선택, 우측 타이틀)로 복원 요청.
- **분석**: 미디어 쿼리를 사용하여 웹과 모바일의 레이아웃 순서를 명확히 분리해야 합니다.

### B. 모바일 달력 상호작용 (DatePicker Mobile UX)
- **현상**: 날짜 선택 후 확인/재설정 버튼을 눌러야 닫히는 구조.
- **사용자 요청**: 날짜를 클릭하면 즉시 업데이트되고 달력 창이 닫히도록 개선 (버튼 삭제).
- **분석**: `handleDateClick` 핸들러에서 날짜 변경과 동시에 `setIsOpen(false)`를 호출하고, UI에서 불필요한 액션 버튼들을 제거합니다. (현재 코드 확인 결과 이미 버튼은 없고 날짜 클릭 시 닫히도록 되어 있으나, 사용자는 추가 버튼 삭제를 언급함. 아마도 다른 버전이거나 잔여 CSS/요소가 있을 수 있음.)

### C. 텍스트 데이터 오류 (Text Data Issue)
- **현상**: "인간과 세상이." 문구 관련 피드백.
- **분석**: `grep` 검색 결과 `3.soul.txt`와 `src/data/soulData.ts`에 해당 문구가 포함되어 있습니다. 특정 구절의 가독성이나 위치 문제일 수 있어 확인이 필요합니다.

### D. 웹 하단 여백 부족 (Web Layout Polish)
- **현상**: 모바일에서는 하단 여백이 확보되었으나 웹에서는 여전히 버튼이 본문을 가림.
- **원인**: `MainContent.tsx`에서 `pb-36`이 적용되었으나 웹의 복잡한 레이아웃에서 충분하지 않거나 다른 요소의 간섭이 있을 수 있습니다.

## 3. 해결 방안 (Proposed Solutions)

1. **헤더 레이아웃 분리**:
    - `Header.tsx`에서 `flex-row-reverse` 혹은 `order` 클래스를 활용하여 모바일(좌:타이틀)과 웹(좌:날짜)의 배치를 다르게 설정합니다.
2. **달력 UX 간소화**:
    - `DatePicker.tsx` 내부의 `renderCalendar` 함수에서 불필요한 하단 버튼 영역이 있다면 완전히 제거하고, 즉시 선택 로직을 확고히 합니다. (현재 코드에는 버튼이 보이지 않으나 재점검)
3. **텍스트 정제**:
    - "인간과 세상이." 문구가 포함된 구절의 줄바꿈이나 가독성을 개선합니다.
4. **웹 여백 강화**:
    - `MainContent.tsx`의 하단 여백을 웹 환경에 맞춰 더 대담하게(`md:pb-48`) 확장합니다.

## 4. 향후 방침 (Future Policy)
- 레이아웃 변경 시 반드시 `Responsive Design`을 고려하여 기기별 의도치 않은 변화를 방지합니다.
- 데이터 텍스트의 경우 원본의 느낌을 살리되 현대적인 가독성을 최우선으로 합니다.
